# Taste

- Prefers native file inputs to be presented as styled buttons (e.g., a "Choose file" outline button with the hidden native input behind it) rather than the default browser file input. Confidence: 0.6
